node_modules/
dist/
.env
.env.local
.env.*.local
.DS_Store
*.log

# TypeScript incremental-build state (regenerated by `tsc -b`)
*.tsbuildinfo

# Compiled vite.config.* (tsc -b emits these into the project root)
vite.config.js
vite.config.d.ts
